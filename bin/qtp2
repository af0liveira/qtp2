#!/usr/bin/env python
import os
import sys

current_dir = os.path.dirname(os.path.realpath(__file__))
parent_dir = os.path.dirname(current_dir)

sys.path.insert(0, parent_dir)
from qtp.qtp2 import qtp2
sys.path.pop(0)

if __name__ == '__main__': 
    qtp2()
